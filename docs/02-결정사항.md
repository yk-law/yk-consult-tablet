# 02. 결정사항 (결정 로그)

> 새 결정은 아래에 날짜와 함께 append 한다. 이전 결정을 삭제하지 말고 "→ 변경됨"으로 표시한다.

## 기기와 역할 분리 (2026-09-04 확정)
- YK-OS = 상담실장이 **데스크탑**에서 접근하는 환경
- 상담 태블릿 = 고객에게 제공되는 **별도 태블릿 스크린**
- **EMPLOYEE SIDE 화면 = YK-OS 좌측 메뉴의 「상담 태블릿」 메뉴**. 현재 이 메뉴에는 *방문 예정 목록*, *상담 내용 작성*이 있고, **온라인 바인더는 여기에 추가된다.**

| | 상담 태블릿 (고객) | YK-OS (상담실장) |
|---|---|---|
| 대기화면·변호사 탐색·업무사례·후기·영상 | O | |
| 전자서명 | O | 계약서를 밀어 넣음 |
| 상담 기록 작성 | X | O |
| 상담 스케줄 관리 | X (제거) | O |
| 화면 공유 | 받음 | 보냄 |

- 태블릿에 **로그인 없음**. 상담실장이 방문 예정 목록에서 건을 열면 세션이 붙는다.
- **전자서명은 태블릿에 유지** (이전 전제에서 변경됨 → 재확정)
- **태블릿의 상담 스케줄 관리는 제거** (이전 전제에서 변경됨). 기존 상담 태블릿 목업의 상담관리 탭 전체가 사라진다.

## 온라인 바인더 (2026-09-03~04)
- 목적: 실물 바인더 대체. 프로필을 하나의 상품으로 보고 맨파워를 노출한다.
- **처분권자와의 지연 확인을 고객 화면에 노출해도 된다** (2026-09-03 확정). 단 문구는 **사실 서술만** — "언제 어디서 근무했다". 관계 암시 표현 금지.
- 추천 로직: 화면에 **점수는 노출하지 않되 이유는 보여준다** (2026-09-04 변경. 그 전에는 "노출 안 함"이었음).
- **후기는 전체 목록이 기본**, 변호사 프로필에서 클릭하면 해당 변호사 후기로 필터.
- **초기 분야 선택 화면에 분야별 변호사 수는 노출하지 않는다.**
- 태블릿 최초 화면 = **YK 로고 + 홈페이지 상단 배너 영상**, 「상담을 시작할게요」 버튼으로 진입.
- **검색 지원** (2026-09-04). 이름·분야·출신·직위·경력으로 검색. 태블릿 진입 화면과 목록, YK-OS 추천표 모두.
- **승패소율은 만들지 않는다** (2026-09-04). 백업 데이터 확보가 안 되어 있고, YK-OS 종결 유형이 형사 중심이라 민사·가사를 표현하지 못한다.
- **최초 화면은 브랜드 영상부터** (2026-09-04). yklawfirm.co.kr 상단 배너 영상(`/video/main_video.webm`, 포스터 `/image/ykVideoPoster.webp`)을 무음·반복으로 전면 재생하고, 그 위에 YK 로고와 「상담을 시작할게요」 버튼을 올린다. 로컬 HTML·실제 태블릿에서는 재생되고, Claude 아티팩트 뷰어에서는 CSP로 외부 미디어가 차단되어 그라디언트 배경으로 대체된다. **실서비스에서는 영상 파일을 자체 호스팅**할 것.
- **변호사 외 고문·전문위원·자문위원도 노출** (2026-09-04). 태블릿 하단 `전문가` 탭. 고문 28 · 전문위원 61 · 자문위원 9 = **98명**. 전체/고문/전문위원/자문위원 필터. 이름 + 경력 라벨(정부기관 경력, 경찰 경력, 고용노동부 경력, 관세 전문 등)만 노출하고 담당 분야 리스트는 넣지 않는다.
- **디자인 톤: 프리미엄 · 정숙** (2026-09-04). 아이보리 지면(#FBFAF7) + 헤어라인 구분선(#E9E5DC), 딥 버건디 포인트(#6E2739), 골드 라벨(#A98A4B), 잉크 텍스트(#171C24). 제목·숫자는 Noto Serif KR, 본문은 Noto Sans KR. 카드 박스·둥근 모서리·그림자를 걷어내고 여백과 선으로 위계를 만든다. 추천 순위는 `01 / 02 / 03` 세리프 숫자.
- **대기 화면에 예약자 이름으로 인사한다** (2026-09-04). 「안녕하세요 ○○○ 님」 + 방문 일시·상담실·사건 종류 한 줄. 목업의 이름은 전부 가명.
- **「상담을 시작할게요」 다음에 '예약 정보 확인' 뎁스를 둔다** (2026-09-04). 바로 추천 목록으로 가지 않는다. 문구: "예약해주신 정보를 기반으로 미리 추천 리스트를 준비했습니다".
- **예약 확인 화면은 YK-OS 콜 탭을 추종한다** — 유형(개인/기업) · 역할(사건 당사자/사건 위임인/추천인/기타) + 사건 당사자와의 관계 · 사건 종류(2단) · 방문 일시/지사. 필드명과 값 도메인을 임의로 바꾸지 않는다.
- **콜 메모(`사건개요(영업전략팀)`)는 고객이 교차검증할 수준으로 요약해 노출한다** (2026-09-04). 문장 단위 6개 안팎, 각 문장에 「맞아요 / 달라요」. 「달라요」는 즉시 YK-OS에 정정 요청으로 표시되고, 상담실장은 상담 시작 전에 어디가 틀렸는지 안다. 원문 그대로 붙여넣지 않는다.
- **태블릿 대기 화면은 '미연결'이 기본이다** (2026-09-04). YK-OS와 연결되기 전에는 **브랜드 영상 + 공식 로고 + 대표 수치**만 띄우고, 인사도 시작 버튼도 없다. 상담실장이 방문 예정 목록에서 「화면 공유」를 눌러야 비로소 「안녕하세요 ○○○ 님」과 「상담을 시작할게요」가 뜬다.
- **로고는 공식 로고를 쓴다** — `/_next/static/media/brand_logo1.4419cb1a.svg` (698×136, 네이비 #1A2A56 + 그레이 #A6A8AA). 어두운 영상 위에서는 화이트 단색으로 반전해 얹는다. 파일명에 해시가 붙어 있어 배포 때마다 바뀌므로 **실서비스에서는 로고 파일을 태블릿에 동봉**할 것. 로드 실패 시 타이포그래피 워드마크로 폴백한다.
- **YK-OS 상담 태블릿 화면은 좌/우 분할이다** (2026-09-04, 실제 화면 확인). 상단 탭으로 들어가는 구조가 아니라 **한 스크린에서 좌측 방문 예정 목록 · 우측 고객 화면**을 같이 본다. 온라인 바인더는 우측 영역에 들어간다.
- **바인더 연결 전 상태를 명시한다** — 우측은 「고객 화면 미연결」 안내(연결 3단계)만 보이고, 좌측 목록은 그대로 동작한다.
- **방문 예정 목록의 고객명·사건·연락처 검색은 유지한다** (현행 YK-OS 기능).
- **좌/우 역할 확정** (2026-09-04) — 좌측이 **상담실장이 보는 화면**, 우측이 **고객 태블릿 연결 화면**이다. 우측은 고객 태블릿을 그대로 비추는 미러링 영역이지 상담실장용 분석 패널이 아니다. 분석·추천·처분권자 접점은 전부 좌측에 둔다.
- **미러링은 보기 전용** — 상담실장이 우측 미러를 직접 조작해 고객 화면을 넘기지 않는다. 고객 화면 전환은 좌측 추천 순위표의 「화면 공유」로만 한다.
- **공유 해제 기능** (2026-09-04) — 세 곳(카드 버튼·좌측 연결 배너·우측 고객화면 탭)에서 해제할 수 있다. 해제하면 태블릿은 대기 화면으로 돌아가고 관심 변호사·검색어가 초기화된다. 다음 고객을 앉히기 전에 이전 고객 정보가 남지 않게 하는 것이 목적.
- **나란히 보기도 좌측이 상담실장** (2026-09-04). YK-OS 내부의 좌/우 규칙과 순서를 맞춘다. 좌 = 상담실장 YK-OS, 우 = 고객 태블릿.
- **교차검증은 실시간으로 상담실장 화면에 호출된다** (2026-09-04). 고객이 「맞아요/달라요」를 누르는 즉시 YK-OS의 해당 문장이 강조되고(2.4초 하이라이트), 헤더에 「고객 확인 중」 표시가 점멸하며, 상단 상태가 `고객 정정 요청 N건`으로 바뀐다. 상담실장은 자리에 앉기 전에 어디가 틀렸는지 안다.
- **교차검증이 불가능한 케이스를 별도로 다룬다** (2026-09-04). 콜 단계에서 "내방해서 직접 설명하겠다"며 사건 내용을 말하지 않는 고객이 있다. 이 경우:
  - YK-OS: **「고객 사전 교차검증 내용 없음」** + 사유를 띄운다. 사실관계를 상담에서 처음부터 들어야 한다는 안내를 붙인다.
  - 고객 태블릿: **교차검증 뎁스를 아예 주지 않는다.** 예약 카드(유형·역할·사건 종류)만 남기고 문구도 "사건 이야기는 상담 때 편하게 말씀해 주세요"로 바꾼다. 확인할 게 없는데 확인 화면을 띄우면 안 된다.
  - 판정 기준은 `BOOKINGS[].memo` 가 비어 있는지 여부. 사유는 `memoNote`.
- **확장 질문 시나리오** (2026-09-04). 사건 종류별로 파생될 수 있는 건과 그것을 확인하는 질문을 상담실장 화면에 띄운다. 예) 이혼 → 양육권·양육비 / 재산분할 / 상간소송 / 면접교섭 / 재산명시·은닉.
- **「상담 내용 작성」은 실제로 동작해야 한다** (2026-09-04). 목록의 버튼을 누르면 현행 YK-OS 모달(`[고객명(역할) - 사건종류] 상담 정보`, 탭 1콜~6종료)이 뜬다. 바인더는 **콜 탭만** 바꾸고 나머지 흐름은 손대지 않는다.
- **바인더 결과가 상담 기록으로 자동 이월된다** — 콜 메모·고객 정정·고객 관심 변호사·약정금 가이드가 각각 사건개요/정정박스/부서 배당 의견/제안 금액 칸에 채워진 상태로 열린다. 상담실장이 바인더에서 본 것을 다시 타이핑하지 않는 것이 이 기능의 목적.
- **위임 사무는 자동으로 채우지 않는다** (2026-09-04). 상담실장이 직접 적을 값이다. 확장 질문 시나리오는 좌측 참고 패널에만 남기고 폼에는 넣지 않는다.
- **예약 확인 이후 경로를 분기한다** (2026-09-04). 「확인함」과 「나중에」가 같은 화면으로 가면 확인 버튼의 의미가 없다.
  - 확인함 → **추천 목록**. 사건이 확정됐으니 추천 3인을 앞세운다.
  - 나중에 → **둘러보기**. 추천은 맨 아래 「참고 추천」으로 내리고 정확도가 낮을 수 있다고 밝힌다. 대신 검색·업무사례·후기·전문가를 앞에 둔다. 상단 배너로 언제든 확인 화면으로 되돌아갈 수 있다.
  - 하단 `변호사` 탭도 고객이 있는 경로를 따라간다. 상담실장 화면에는 고객이 어느 쪽을 택했는지 표시된다 — 확인을 미뤘다는 것 자체가 상담에서 알아야 할 신호다.
  - **YK-OS 전용. 고객 태블릿에는 노출하지 않는다** — 고객에게 사건을 늘려 파는 것으로 보이면 안 된다.
  - 각 항목에 「물어보기 → 확인함」 토글. 확인한 항목은 상담 기록의 **위임 사무**에 반영한다.
  - 데이터는 `app/data/scenarios.js`. 키는 사건 종류 소분류 우선, 없으면 대분류 폴백.
  - **실제로 존재하는 건만 담는다** — 없는 사건을 만들어 붙이는 용도가 아니다. 내방 대비 선임율을 올리는 것이 목적이지 건수를 부풀리는 것이 목적이 아니다.
- **여러 예약자 중에서 골라 화면 공유한다** — 목록의 어느 행이든 「화면 공유」를 누르면 그 예약으로 태블릿이 갈아끼워진다. 추천 로직·비용 기준도 그 예약의 사건 종류를 따라 바뀐다.
- **대기 화면 카운터는 검증된 수치만 쓴다** — 변호사 318 · 고문·위원 98 · 전국 사무소 31. 근거를 찾지 못한 「업무사례 1,839」는 제거했다(`docs/04` 참조).

## 추천 점수 (가중치 미확정 — 현재 목업 값)
| 항목 | 점수 |
|---|---|
| 사건 분야 정확 일치 | 100 |
| 인접 분야 보유 | 개당 8 (최대 24) |
| 관할기관 근무 이력 | 40 |
| 직위 | 대표 10 / 파트너 6 / 고문 4 |
| 상세 프로필 보유 | 6 |
| 담당 분야 폭 | ×1 (동점 처리) |

고객 화면에는 위 점수를 숨기고 다음 형태의 칩만 노출한다:
`민사 담당` `서울중앙지방법원 근무 이력` `관련 분야 부동산·건설 · 행정` `부장판사 출신`

## 방문 예정 목록 기간 필터 (2026-09-08)
- 날짜 `‹ 오늘 ›` 이동을 **오늘 / 내일 / 이번주 / 다음주** 버튼으로 바꾼다. 기본값은 오늘.
- **이번주·다음주는 월요일 시작**(일요일이 주의 마지막). 시연 기준일은 YK-OS 시계와 같은 `2026-09-04`.
- 카드 일시에는 **요일을 넣는다.** 예) `26-09-04 금 16:00`. 요일은 딥 버건디로 구분한다. 요일은 날짜에서 계산한다(시연 기준일 `2026-09-04`는 금요일).
- **상담실장 목록 가독성** (2026-09-08) — 필터·일시·이름·상태를 한 단계 키우고(`11px`→`13~16px`), 보조색(`--mut`) 대신 `--ink`/`--ink2`를 쓴다. 고객 태블릿 타이포는 그대로 둔다. → 변경됨 (2026-09-10, 고객 태블릿도 프리미엄 타이포로 올림)

## 고객 태블릿 프리미엄 톤 (2026-09-10)
- 고객 화면만 손댄다. YK-OS 실무 UI는 그대로.
- 지면은 아이보리, 제목은 세리프, 구분선은 골드 헤어라인. CTA는 알약이 아니라 **각진 버건디 + 골드 테두리**.
- 본문 웨이트 300을 400으로 올리고 글자 크기를 한 단계 키운다. 카드 박스·그림자로 위계를 만들지 않는다.

## 계약서 (Confluence 278790188)
- 계약서 변수는 YK-OS 등록 데이터를 불러와 반영. 작년/올해 민사 계약서 내용이 달라 **템플릿화 + 버전 관리** 필요.
- 변수 대상: p1 사건(사건번호·사건명·의뢰인·상대방·관할·소송물가액), p1 보수조건, p1 서명일, p2 제6조 착수보수, p2 제7조 성과보수, p3 제8조 출장일당·예치금
- 특약사항은 본문을 서버 저장하고 포함 여부를 Y/N로 결정
- **개인정보 수신이용동의 계약서 추가 필요** (마케팅 수신동의 포함)

## 선임 계약 서식 (2026-09-10)
- 출처: YK-OS staging `/#/app/visit/sign` 기본서식(소송위임계약서). **실제 eng 건의 인적정보는 넣지 않는다.**
- 종이 본문(조문·표)은 원본 서식을 유지한다. 상담실장 UI만 현행 화면 블록을 따른다.
- 작성 화면 블록: **사건** / **약정 조건**(위임사무·특약) / **보수 조건**(착수금·성공보수·출장비) / **부서 배당**(희망 배당 부서 1~3 · 의견).
- 템플릿 탭은 `형사` / `민·가사`. 착수금은 빼지 못한다. 태블릿 전송 전 희망 배당 부서 1과 배당 의견이 필요하다.
- 착수금 시연 금액은 약정금 중앙값(`FEE`). 주민번호·실주소는 비운다.

## 시연본 배포 (2026-09-15)
- 용도는 **법인 내부 보고·시연**. 외부 공개용이 아니다.
- 임시 터널(`trycloudflare.com`)은 **쓰지 않는다.** 약 24시간마다 만료되고 그때마다 주소가 바뀐다. 5회 연속 끊긴 것을 확인했다.
- 배포물은 **단일 HTML 한 장** — `dist/index.html`. 정적 호스팅에 올리거나 파일째 열어도 동작한다.
- 빌드는 `python3 tools/build-static.py`. 이 장비에 node가 없어 파이썬으로 뽑는다. `tools/build.mjs`는 아티팩트 임베드용이라 `<html>` 껍데기를 벗기므로 호스팅에는 쓸 수 없다.
- 웹폰트(Google Fonts)와 브랜드 영상(`yklawfirm.co.kr`)은 외부 링크로 남긴다. 망분리 환경에서는 기본 폰트로 떨어지고 영상만 빠진다. 인물 사진은 base64라 영향 없다.
- `dist/`는 git 미추적. **배포 전 `app/`을 고치고 다시 빌드한다.** `dist/`를 직접 손대면 덮어써진다.

## 스택 교체: React + FastAPI (2026-09-16)
- 기존 vanilla HTML/CSS/JS 프로토타입을 **대체**한다. 화면 구조·추천 가중치·기기 분리 전제는 그대로다.
- **프론트** `app/` — Vite 5 + React 18. 고객 태블릿과 YK-OS는 컴포넌트로 나뉜다. `?v=tab|os|both` 는 로컬 보기만 바꾼다.
- **API** `server/` — Python FastAPI. 카탈로그·추천 점수·계약 초안·세션이 여기서 계산된다. 세션은 메모리 한 건. 태블릿과 YK-OS가 `GET /api/session` 을 700ms 폴링해서 같은 상태를 본다.
- 목업 JSON은 `server/data/*.json`. vanilla JS 데이터를 `tools/export-json.mjs` 로 옮겼다. 예전 소스는 `vanilla/` 에 비교용으로만 둔다.
- 개발: `node tools/dev.mjs` → API `http://127.0.0.1:8000`, 화면 `http://localhost:5173` (`/api` 프록시).
- GitHub Pages 정적 스냅샷은 이 스택과 별개다. API가 없으면 화면 공유가 동기화되지 않는다.

## 태블릿은 고객 View다 (2026-09-17 회의 — 디지털 바인더)
출처: Confluence `YKP / 311623720` 「디지털 바인더 09월 17일 회의」.

이전 작업(YK-OS 좌/우 미러링 · 화면 공유 · 맞아요/달라요 · 추천 3인 선택)은 **현업 프로세스와 괴리가 있다**고 합의했다. → 아래는 그 전제를 **변경함**.

- **YK-OS 「상담 태블릿」 메뉴 프레임에 구속하지 않는다.** 고객 화면을 상담실장이 조작하는 구조는 기대하지 않는다. 상담실장은 계약 성사에 집중한다.
- **지류 바인더 대체는 주목적이 아니다.** 변호사를 검색해 프로필을 보여주는 수요는 작다. 병렬로만 남을 수 있다.
- **태블릿의 역할은 유저에게 View.** 고객이 담당 변호사를 고르거나, 사건 사실의 정답/일치를 탭하는 과정은 두지 않는다.
- **보여줄 재료**
  1. 최초 방문 대기 — 브랜드 필름 `https://ykos.yklawfirm.co.kr/assets/yk-brand-film.mp4`
  2. 해당 예약의 AI 리포트 **사건개요** (내방 고객별). 참고 화면: YK-OS `/#/app/conturning/helper/`
  3. 전관(대표·파트너 중 부장판사·부장검사 이상) 프로필과 영상. 영상 재생목록: YouTube `PL0dUw0MrEgaospa1FtIbfR6w1Hmph9NR4`. 영상 없는 사람은 홈페이지 프로필 기준으로 노출.
- **현업 순서에 재료를 끼운다.** 방문 당일 아침 면담에서 **담당 변호사는 이미 지정**되어 있다.
  - 고객 대기: 대기 영상 → 사건요약 보고서
  - 상담실장 입실: 사실관계는 대화로 확인 (태블릿에서 맞아요/달라요를 받지 않음)
  - 변호사 입실: **오늘의 상담 변호사 프로필**만 보여 줌
  - 가격전략으로 자리를 비운 사이: 법원·검찰 출신 대표·파트너 영상·프로필
  - 재입실 후 계약 영업: 상담실장이 구두로 진행. 태블릿은 구성원 View를 유지
- **고객 화면 카피에서 '전관'이라는 말로 관계를 암시하지 않는다.** 근무 이력 사실만 쓴다. 승패소율은 만들지 않는다.
- 시연 앱은 **고객 태블릿만** 그린다. YK-OS 연동·미러링·화면 공유 UI는 이 목업에 두지 않는다.

## 상담 만족도 설문 (2026-09-17)
- 출처: Claude 아티팩트 `상담만족도_설문조사_샘플`. 태블릿 장면 `설문` (`?s=survey`).
- **음성 녹취 동의 문항은 넣지 않는다.** 아티팩트의 「상담 내용을 음성으로 남겨…」 질문을 삭제했다.
- 자리는 비운 사이(가격전략 구간)에 고객이 태블릿으로 응답한다. 결과는 상담 변호사·실장에게 공개되지 않는다고 안내한다.
  → **변경됨 (2026-09-18)** 타이밍을 **상담 완료 후**로 옮긴다. 한 장에 계약 전·후 문항을 섞지 않는다.
  - **상담 후 · 계약 전**: 먼저 선임 / 미선임을 고른다. 공통은 예약·대기·설명·만족. 선임은 비용·범위 설명과 선임 후 기대. 미선임은 이유·재연락. 「계약을 진행하신다면」처럼 아직 결정 전인 질문을 선임 전제에 두지 않는다.
  - **계약 후**: 계약을 마친 분만. 계약서 이해·진행 편의·이후 연락 안내·착수 후 받고 싶은 안내. 미선임은 이 설문이 없다.
  - 「승소」 기대 문항은 쓰지 않는다. 녹취 문항은 계속 넣지 않는다.

## 태블릿 내방 흐름 재구성 (2026-09-17)
이전 대기 화면(영상 위에 로고·인사를 얹어 opacity를 낮춘 구성)은 **영상이 잘 안 보인다는 이유로 변경함**.

1. **예약자가 맞으신가요?** — 가명·방문 일시·상담실·사건 종류. 「네, 맞습니다」만 다음으로. 「아닙니다」는 상담실장에게 말씀해 달라는 안내만.
2. **상담을 준비 중입니다** — 약 2초 뒤 브랜드 필름.
3. **브랜드 필름 전면 재생** — 배경이 아니라 화면 자체. 루프하지 않는다. 시청이 끝나면 사건요약으로 바로 간다.
4. **사건요약** — YK-OS `/#/app/conturning/helper/` 의 **사건개요** 블록 UI(상담예약 AI 리포트 헤더·사건개요 탭·메타 표·사실관계). 시연은 예약별 가명 메모. 실의뢰인 원문은 넣지 않는다. 라이브 `/v1/` 연동은 로그인 뒤에 붙인다. → **변경됨 (2026-09-18)** 원문은 로그인 없이 긁지 않는다. 제공된 사건개요 원문을 가명 가공해 `overview` 네 섹션(당사자 관계·사실 관계·주장·조력)으로 보여 준다.
5. **10분 뒤 오늘의 상담 변호사** — 사건개요를 띄운 뒤 10분(기본 600초, 시연은 `?wait=` 초) 후 자동으로 지정 변호사 프로필. 태블릿용 다크·골드 히어로 레이아웃. → **변경됨 (2026-09-17)** 시간을 세지 않는다. 사건요약 아래 「오늘 상담변호사 확인하기」를 눌러 프로필로 간다.
6. **구성원** — 전원 나열이 아니다. 오늘 사건 분류(분야 일치 → 관할 근무 → 인접)에 맞는 법원·검찰 출신 대표·파트너만 최대 5명. 카드는 스페셜 등급 포일로, 대법관/고검장·검사장/부장판사·부장검사 시각만 나눈다. 고객 카피에 **전관 관계**라는 말은 쓰지 않고 근무 이력 사실만 쓴다. → **변경됨 (2026-09-18)** 시연 탭 이름은 **전관**. 사진은 원본 비율, 직위 태그는 사진에 붙이지 않고 프레임으로 등급을 나눈다. N명 `auto-fill` 그리드. 본문에 「전관 관계」는 쓰지 않는다.

## 가로 태블릿 · 내방 조작 (2026-09-17)
- 시연 프레임은 **가로(16:10)** 로 잡는다. 세로로 길쭉한 목업을 쓰지 않는다. → **변경됨 (2026-09-17)** 아래 「실기기 반응형」.
- 예약 확인 화면은 제목·본문 글자를 키워 읽기 쉽게 한다.
- 「아닙니다」는 줄을 나눠 안내하고 **뒤로가기**로 예약 확인으로 돌아간다.
- 브랜드 필름에는 **건너뛰기**를 처음부터 둔다.

## 실기기 반응형 (2026-09-17)
이전 시연 셸의 **고정 16:10 `aspect-ratio` 박스**는 아이패드 4:3, 11인치(~1.43), 12.9, 안드로이드 16:10에서 레터박스가 심해 **쓰지 않기로 변경함**.

- **가로를 기본**으로 하되 스테이지 너비를 채운다. 높이는 `min(뷰포트, 사용 가능 영역)`. max-width는 `min(1440px, 100%)`. 한 비율에 가두지 않는다.
- 글자·여백은 `clamp`와 컨테이너 쿼리(`cqw`/`cqh`). 태블릿 컨테이너 너비 1080 이하, 또는 높이 680 이하에서 상담변호사·구성원 **2단을 1단**으로 접고, 짧은 높이에서는 변호사 영상을 숨긴다.
- 확인·준비·필름·사건요약·변호사·구성원·설문이 같은 규칙으로 리플로우한다. 필름 **건너뛰기**는 safe-area 안쪽, 처음부터 도달 가능.
- 시연 셸은 전형적인 가로 태블릿처럼 보이게 두되, 뷰포트를 넘기지 않는다. YK-OS 미러링·화면 공유 UI는 넣지 않는다.

## 인물 사진 프레임 · 전관 탭 (2026-09-18)
- 바인더 인물 사진은 세로로 가늘다(대략 190–340 × 480). 좌측 컬럼·카드 높이에 `object-fit:cover`로 맞추면 늘려 보이므로 **원본 비율을 유지**한다. 늘리거나 잘라 채우지 않는다.
- 오늘의 상담 변호사는 히어로 풀블리드를 쓰지 않는다. 사진 크기는 원본에 맞춰 두고, 골드 이중 매트 프레임만 두른다.
- **사진 위에 부장판사·출신 직위 태그를 붙이지 않는다.** 직위는 이름 옆 문장으로만 적는다.
- 시연 탭 이름 **구성원 → 전관**으로 변경. 고객 본문에 「전관 관계」라는 말은 여전히 쓰지 않는다.
- 전관 화면은 N명을 가정한 `auto-fill` 그리드. 1명이 한 줄을 차지하지 않는다. 등급(대법관 / 고검장·검사장 / 그 외)은 **사진 프레임**으로만 나눈다.

## 초기화면 사건분류 제거 · 전문인력 (2026-09-18)
- 예약 확인(초기화면)에서 **사건 분류를 빼다.** 방문 일시·상담실만 확인한다. 사건 종류는 사건요약 이후에만 본다.
- 고객 화면 탭 이름 **전관 → 전문인력**. 「전관」은 내부 정의(`is_senior`)로만 남긴다.
- 카드 한 줄 카피는 홈페이지 구성원 프로필 명언을 쓴다. 예: 김동진 `/member/lawyer/6254` 「어려움에 처한 의뢰인을 정성으로 돕겠습니다.」 출신 직위로 만든 「재판부를 이끄신 이력」 문구는 쓰지 않는다.
- 한상진 사진은 바인더 13장에 없어 비어 있었다. 홈페이지 `/upload_file/lawyer/한상진.png` 와 `/member/lawyer/6071` 경력·명언을 반영했다. 사진이 없던 배성범·양호산·최영운·천기홍도 같은 경로로 채웠다.
- **프레임 우열 (변호사)** 대표변호사 > 고문변호사 > 파트너변호사 > 변호사.
- **고문 / 전문위원 / 자문위원** 은 변호사 골드와 다른 실버·네이비·아이보리 프레임을 쓴다.

## 전문인력 대표 경력 · 고정 카드 (2026-09-18)
- 카드에서 가장 크게 보이는 문구는 홈페이지 **대표 경력** 뱃지다. 예: 권순일 `/member/lawyer/5806` 「대법관 역임」「판사 역임」. 김동진 `/member/lawyer/6254` 「부장판사 역임」. `lawyers.json` `d.titles`.
- 전문인력 그리드는 **같은 칸 크기**다. 사진 프레임 바깥 상자 220px 높이, 사진은 상자 안에서 원본 비율 `contain`. 카드 본문(대표 경력 2줄·이름 1줄·명언 2줄)도 높이를 맞춘다. 직위 우열은 테두리 색만으로 나눈다.

## 전문인력 소개 문구 (2026-09-18)
- 헤드 「법무법인 YK의 전문인력입니다」 바로 아래에 「고객님의 사건을 성심껏 맡아드리기 위한 전담인력을 소개합니다」를 둔다. 헤드를 대체하지 않는다.
- 기존 프레임 설명(직위는 사진 프레임으로 구분…)은 그 아래 보조 카피로 유지한다.
- 고객 화면에 「전관」은 쓰지 않는다.

## 사건요약 고객향 어체 (2026-09-18)
이전 사건요약은 YK-OS helper 원문 구조를 따라 「의뢰인은 ~라고 진술」 식의 내부 보고체였다. → 변경함.

- **보는 사람은 고객이다.** 상담실장 메모가 아니라, 예약 때 말씀하신 내용을 고객에게 다시 설명해 주는 어체로 쓴다.
- 「의뢰인」「내방인」「진술」「콜 단계」를 고객 화면에 쓰지 않는다. 「고객님께서는 ~라고 말씀하셨습니다 / 이렇게 정리했습니다」. 반말(너)은 쓰지 않고 정중한 하십시오체.
- 네 칸 구조는 유지하되 제목을 고객향으로 바꾼다. **관련하신 분들 / 지금까지의 경과 / 고객님의 입장 / 오늘 상담에서 확인하고 싶은 점.**
- 화면 제목은 「사건 개요」가 아니라 「말씀하신 내용을 이렇게 정리했습니다」. 출처 줄에 YK-OS·AI 리포트라는 내부 시스템명을 쓰지 않는다.
- 사건 내용이 비어 있는 예약(김서준)은 「콜 단계에서 받아두지 못했습니다」가 아니라, 오늘 상담에서 편하게 말씀해 달라는 안내만 둔다.
- 처분권자 접점·확장 질문·「전관」·근거 없는 수치는 넣지 않는다. 목업 이름은 가명.

## 브랜드 필름 건너뛰기 · 상체 사진 · 고문·위원 (2026-09-22)
- **건너뛰기는 처음부터 둔다** (2026-09-17 유지). 영상 위에 버튼 박스를 올리지 않는다. 가장자리·safe-area의 **얇은 텍스트**, 약 1.6초 지연 페이드, 기본은 낮은 대비. 호버·포커스·탭 시에만 또렷해진다. → **변경됨 (2026-09-22)** 대비·글자·골드 밑줄을 올려 탭 타깃이 보이게 한다. 위치는 가장자리 유지, 처음부터 도달.
- **인물 사진은 상체 크롭.** 전신 contain을 쓰지 않는다. 홈페이지 구성원 상세(예: 김동진 `/member/lawyer/6254`) 하단과 같이 머리~상체가 보이게 `object-fit:cover` + `object-position:center 12%`. 상담변호사·전문인력 전원 동일.
- 오늘의 상담 변호사는 히어로가 화면을 잠그지 않는다. `.counselpane`이 스크롤되고, 명언·경력·영상이 잘리지 않는다. 사진은 텍스트를 밀거나 큰 공백을 만들지 않게 상단 정렬.
- 전문인력 카드는 **232×428 고정**을 유지한다. 사진 칸은 상체 크롭에 맞춰 200px. 카드끼리 크기가 달라지면 안 된다. → **변경됨 (2026-09-22)** 사진 칸 178px·패딩·명언 2줄 높이로 맞춰 하단 잘림을 막는다. 카드 외형 크기는 232×428 유지.
- **고문·전문위원·자문위원 카드 사진**을 홈페이지에서 채운다. 시연 3인: 강성용(`/member/advisory/6148`) · 강신선(`/member/expert/6018`) · 김한기(`/member/consultant/6023`). 직위·대표 경력·소개만. 고객 화면에 「전관」 없음. 이메일·전화·생년월일은 넣지 않는다.
- 이전 「원본 비율 contain, 늘리거나 자르지 않음」(2026-09-18)은 상체 크롭으로 **변경됨**.

## 전문인력 나열 순서 · 프레임 액자화 (2026-09-22)
- **그리드 나열 순서**를 바꾼다: 대표변호사 > 파트너변호사 > 고문변호사 > 고문 > 전문위원 > 자문위원. 같은 직위 안에서는 기존 추천 점수 순. `server/catalog.py` `LIST_RANK` / `seniors_for`.
- **프레임 색 우열은 유지** (2026-09-18): 대표 > 고문변호사 > 파트너 > 변호사. 나열 순서를 바꿨다고 프레임 색 위계를 뒤집지 않는다.
- 프로필 프레임은 테두리만 씌운 느낌이 아니라 **이중 매트·코너·얇은 인셋**의 액자처럼 보이게 한다. 그림자·둥근 모서리로 위계를 만들지 않는다. 직위별 색 구분은 유지.

## 건너뛰기 노출 · 전문인력 사진·글 한 판 (2026-09-22)
- 브랜드 필름 **건너뛰기**는 영상을 시작한 뒤 **2초**에 나타난다. 그 전에는 영상 위에 마우스를 올리면 미리 보인다. 가장자리 텍스트는 유지한다.
- 전문인력 카드는 232×428 세로 칸을 **372×176** 가로 한 판으로 바꾼다. 상체 사진과 대표 경력·이름·직위·소개가 같은 높이에서 맞닿게 둔다. 카드끼리 크기는 같다.
- 상세 팝업도 사진과 이름 블록을 한 줄로 맞추고, 경력은 그 아래 이어 쓴다.
- 고문·위원 소개가 뱃지와 같으면 홈페이지 경력 첫 줄을 쓴다. 강성용·강신선·김한기 사진은 `portraits.json`에 있다.

## 정적 시연 링크 (2026-09-22)
- 로컬은 그대로 FastAPI + Vite다.
- 밖에서 여는 주소는 GitHub Pages다. `python tools/snapshot.py`가 예약별 visit JSON을 만들고, Pages에서는 `/api` 대신 그 파일을 읽는다. localhost에서는 API를 그대로 쓴다.
- 주소는 `https://yk-law.github.io/yk-consult-tablet/`. 임시 터널은 쓰지 않는다.
- 카드 본문(대표 경력·이름·직위·명언) 폰트·행간·대비를 올려 읽기 쉽게. 대표 경력은 골드 세리프로 가장 두드러지게.